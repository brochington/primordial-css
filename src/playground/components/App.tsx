import React, { FC } from 'react';

import Routes from 'components/Routes';

type Props = {
  
};

const App: FC<Props> = (props: Props) => {
  const {  } = props;

  return (
    <Routes />
  );
};

export default App;
