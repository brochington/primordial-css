import React, { FC } from 'react';

type Props = {
  
};

const ColorsPage: FC<Props> = (props: Props) => {
  const {  } = props;

  return (
    <div>ColorsPage</div>
  );
};

export default ColorsPage;
