import React, { FC } from 'react';

type Props = {
  
};

const InputsPage: FC<Props> = (props: Props) => {
  const {  } = props;

  return (
    <div>InputsPage</div>
  );
};

export default InputsPage;
