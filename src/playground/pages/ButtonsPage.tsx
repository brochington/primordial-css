import React, { FC } from 'react';

type Props = {
  
};

const ButtonsPage: FC<Props> = (props: Props) => {
  const {  } = props;

  return (
    <div>ButtonsPage</div>
  );
};

export default ButtonsPage;
